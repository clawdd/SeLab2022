package de.unisaarland.cs.se.selab.gamelogic;

/*
import de.unisaarland.cs.se.selab.actioncommand.ActionCommand;
import de.unisaarland.cs.se.selab.actioncommand.ActionFactoryImplementation;
import de.unisaarland.cs.se.selab.comm.BidType;
import de.unisaarland.cs.se.selab.comm.ServerConnection;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;*/

/**
 * This class tests all functionalities implemented in Bid
 */
class BidTest {
/*
    static final ServerConnection<ActionCommand> SC = new ServerConnection<>(
            5006, 1, new ActionFactoryImplementation());
    final DungeonLord julian = new DungeonLord(
            "Julian", 20, 50,
            0, 5, 5, 5, 5,
            SC, null);
    final Bid bidFood = new Bid(BidType.FOOD, 1, julian);
    final Bid bidGold = new Bid(BidType.GOLD, 1, julian);
    final Bid bidRoom = new Bid(BidType.ROOM, 1, julian);
    final Bid bidMonster = new Bid(BidType.MONSTER, 1, julian);
    final Bid bidImp = new Bid(BidType.IMPS, 1, julian);
    final Bid bidNiceness = new Bid(BidType.NICENESS, 1, julian);
    final Bid bidTrap = new Bid(BidType.TRAP, 1, julian);
    final Bid bidTunnel = new Bid(BidType.TUNNEL, 1, julian);

    @Test
    void checkBidTypeFood() {
        Assertions.assertEquals(BidType.FOOD, bidFood.getBidType());
    }

    @Test
    void checkBidTypeGold() {
        Assertions.assertEquals(BidType.GOLD, bidGold.getBidType());
    }

    @Test
    void checkBidTypeRoom() {
        Assertions.assertEquals(BidType.ROOM, bidRoom.getBidType());
    }

    @Test
    void checkBidTypeMonster() {
        Assertions.assertEquals(BidType.MONSTER, bidMonster.getBidType());
    }

    @Test
    void checkBidTypeImp() {
        Assertions.assertEquals(BidType.IMPS, bidImp.getBidType());
    }

    @Test
    void checkBidTypeNiceness() {
        Assertions.assertEquals(BidType.NICENESS, bidNiceness.getBidType());
    }

    @Test
    void checkBidTypeTrap() {
        Assertions.assertEquals(BidType.TRAP, bidTrap.getBidType());
    }

    @Test
    void checkBidTypeTunnel() {
        Assertions.assertEquals(BidType.TUNNEL, bidTunnel.getBidType());
    }
*/
}